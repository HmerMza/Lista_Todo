const CompFooter = () => {
    return (
        <section className="text-center mt-8 rounded-md dark:text-gray-500 duration-1000">
            Drag and drop to reorder list
        </section>
    );
};

export default CompFooter;
